/**\n * Frontend-spezifische Konfiguration\n * Importiert die zentrale Konfiguration und erweitert sie für Frontend-spezifische Bedürfnisse\n */\n\nimport { config as centralConfig, getServiceUrl } from '../../../config/environment';\n\n// Frontend-spezifische Konfiguration\nexport const config = {\n  ...centralConfig,\n  \n  // Frontend-spezifische URLs\n  api: {\n    baseUrl: getServiceUrl('backend'),\n    videos: `${getServiceUrl('backend')}/api/videos`,\n    projects: `${getServiceUrl('backend')}/api/projects`,\n    audioStems: `${getServiceUrl('backend')}/api/audio-stems`,\n    search: `${getServiceUrl('backend')}/api/search`,\n    folders: `${getServiceUrl('backend')}/api/folders`,\n    health: `${getServiceUrl('backend')}/api/health`,\n  },\n  \n  // Service-URLs für externe Services\n  services: {\n    audioService: getServiceUrl('audioService'),\n    audioSeparationService: getServiceUrl('audioSeparationService'),\n    analyzer: getServiceUrl('analyzer'),\n    saliencyService: getServiceUrl('saliencyService'),\n    visionService: getServiceUrl('visionService'),\n  }\n};\n\n// Hilfsfunktionen für Frontend\n\nexport const getApiUrl = (endpoint: string): string => {\n  return `${config.api.baseUrl}${endpoint}`;\n};\n\nexport const getVideoUrl = (videoId: string): string => {\n  return `${config.api.videos}/${videoId}/file`;\n};\n\nexport const getCoverImageUrl = (videoId: string, startTime: number): string => {\n  return `${config.api.videos}/${videoId}/thumbnail?t=${Math.floor(startTime)}`;\n};\n\nexport const getAudioStemUrl = (stemId: string): string => {\n  return `${config.api.audioStems}/${stemId}/stream`;\n};\n\nexport const getProjectUrl = (projectId: string, endpoint: string = ''): string => {\n  return `${config.api.projects}/${projectId}${endpoint}`;\n};